// src/version.js (served at public/js/version.js)
//
// Single source of truth for the version label shown on Settings and the
// Brand View picker. Bump this alongside sw.js's CACHE_VERSION on every
// delivered build — same convention as Corn Plot Harvest, but this app
// numbers itself independently starting at v1.

export const APP_VERSION = "v2.3 (Beta)";
